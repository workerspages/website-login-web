# cronjob

# 每天凌晨 3 点执行登录脚本
# 格式: 分 时 日 月 周
0 3 * * * root python /app/main.py > /var/log/cron.log 2>&1

# 为了方便测试，你也可以设置为每5分钟执行一次
# */5 * * * * root python /app/main.py > /var/log/cron.log 2>&1

# 注意：文件末尾必须有一个空行
